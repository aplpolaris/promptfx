---
groupId: text-classify
defaults: { category: text }
prompts:

  - id: text-classify/sentiment@1.0.0
    title: Classify Sentiment
    template: |
      Classify the sentiment in this text as {{instruct}}:
      ```
      {{{input}}}
      ```